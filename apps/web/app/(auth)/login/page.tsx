/**
 * Login screen (docs/05-UI-UX-DESIGN.md §8: email + password, tenant-
 * branded, MFA per tenant policy). Built out in Phase 1 alongside auth/RBAC.
 */
export default function LoginPage() {
  return (
    <main className="mx-auto max-w-sm p-8">
      <h1 className="text-xl font-bold text-text">Sign in</h1>
      <p className="text-[12.5px] text-text-dim">Auth arrives in Phase 1.</p>
    </main>
  );
}
